{{#each steps}}
  {{#if this.intent}}
    <div class="d-flex justify-content-start">
      {{> userStep userId=../userId storyName=../storyName role="user"}}
    </div>
  {{else}}
    <div class="d-flex justify-content-end">
      {{> botStep userId=../userId storyName=../storyName role="user"}}
    </div>
  {{/if}}
{{/each}}